# Copy this file to .env and fill in your own value
# Never commit the real .env file to source control

# Admin API key required for upload endpoints:
# - POST /api/versions/create-with-files
# - POST /api/upload
ADMIN_API_KEY=change-me-to-a-strong-random-string
